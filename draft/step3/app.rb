# =====================================================================
# 【step3・穴埋め版】届いた言葉によって「返す内容を変える」プログラムです。
#
# step2 は何を送られても同じ内容を返すだけ（オウム返し）でしたが、
# step3 では中身を見て、返事を切り替えます。
#
#   ・「おみくじ」と送られたら  → omikuji の結果を返す
#   ・それ以外が送られたら      → step2 と同じくオウム返し
#
# ★このファイルは未完成です★
#   ______ となっている所を Ruby のコードで埋めると動きます。
#   TODO は全部で 8 個あります。上から順番に埋めていきましょう。
#
# このステップで練習する Ruby の基礎:
#   TODO(1) 配列（はいれつ）を作る
#   TODO(2) 関数を呼び出して、返ってきた値を変数に入れる
#   TODO(3) 文字列のメソッド（.strip）を使う
#   TODO(4) if で場合分けをする
#   TODO(5) 関数の「返り値」（return を書かない書き方）
#   TODO(6) else のときの返り値
#   TODO(7) 配列のメソッド（.sample）でランダムに1つ選ぶ
#   TODO(8) 文字列に変数を埋め込む（#{ } と改行 \n）
#
# 実行方法（ターミナルで）:
#   cd draft/step3
#   ruby app.rb
#   （すでに別の step のフォルダにいる場合は、先に cd ../../ でルートに戻ってください）
#   → その後、Codespaces で公開されたURLを LINE の Webhook に登録します
#     （くわしくは draft/step3/README.md を参照）
#
# 答えを見たいときは ../../complete/step3/app.rb を開いてください。
# =====================================================================

# ---- 使う道具（ライブラリ）を読み込みます ----
# ここは完成済みです。使う道具は step2 とまったく同じです。

# sinatra は、小さな Web サーバーを作るための道具です。
require "sinatra"
# net/http / uri / json は、LINE への送信に使います。
require "net/http"
require "uri"
require "json"
# openssl と base64 は、届いた連絡が本当に LINE からのものか
# 確認する「署名チェック」に使う道具です。
require "openssl"
require "base64"
# dotenv は、.env ファイルを読み込んで ENV に入れてくれます。
# リポジトリルートの .env を直接指定して読み込みます。
require "dotenv"
Dotenv.load(File.expand_path("../../.env", __dir__))

# ---- 秘密の設定を環境変数から取り出します ----
# ここも完成済みです。

# メッセージを送る（返信する）ための鍵です。
CHANNEL_ACCESS_TOKEN = ENV["LINE_CHANNEL_ACCESS_TOKEN"]
# 届いた連絡が本物か確認するための、もう1つの鍵(シークレット)です。
CHANNEL_SECRET = ENV["LINE_CHANNEL_SECRET"]

# ---- おみくじの中身を用意します ----

# ----------------------------------------------------------------------
# TODO(1) おみくじの結果を「配列」で用意してください。
#
#   [ ] で囲んだものを「配列(はいれつ)」と呼びます。
#   たくさんの値を、順番に並べてまとめて持っておける入れ物です。
#   ここに書いた中から1つが選ばれます。
#
#   書き方（値と値のあいだは「,」で区切ります）:
#     [
#       "りんご",
#       "みかん",
#       "ぶどう"
#     ]
#
#   まずは次の7つを入れてみましょう（増やしても減らしてもOKです）:
#     大吉 / 中吉 / 小吉 / 吉 / 末吉 / 凶 / 大凶
#
#   ※ 文字は "（ダブルクォート）で囲みます
# ----------------------------------------------------------------------
OMIKUJI_RESULTS = ______

# ---- サーバーの待ち受け設定 ----
# ここは完成済みです。おまじないだと思って先に進みましょう。

# どのパソコンからの接続も受け付ける、という意味の設定です。
# （Codespaces で外から届くようにするために必要です）
set :bind, "0.0.0.0"
# 待ち受けるポート番号です。4567 は sinatra の初期値です。
set :port, 4567
# どの住所(ホスト名)からのアクセスも許可する、という設定です。
# sinatra は安全のため、初期状態だと localhost 以外を
# 「Host not permitted」と拒否します。Codespaces の公開URL
# （...app.github.dev）で受け取れるように、この制限を外します。
set :host_authorization, { permitted_hosts: [] }

# ---- 動作確認用のページ ----

# ブラウザで「/」を開いたときに表示される簡単な文字です。
# サーバーが動いているかの確認に使えます。
get "/" do
  "おみくじBot は動いています（step3）"
end

# ---- LINE からの連絡(Webhook)を受け取る入り口 ----
# ここは step2 で書いた内容と同じなので、完成させてあります。
# （step2 の復習として、目で追ってみましょう）

# LINE は「/callback」という住所に POST で連絡してきます。
post "/callback" do
  # 届いた本文（まだ文字のかたまり）を読み取ります。
  body = request.body.read

  # まず、この連絡が本当に LINE からのものかを確認します。
  # にせものを受け付けないための、大切なセキュリティ確認です。
  unless valid_signature?(body, request.env["HTTP_X_LINE_SIGNATURE"])
    halt 400, "署名が正しくありません"
  end

  # 文字のかたまり(JSON)を、Ruby が扱いやすい形に変換します。
  data = JSON.parse(body)

  # 1回の連絡に複数の出来事(events)が入っていることがあるので、
  # each で1つずつ順番に処理します。
  data["events"].each do |event|
    # メッセージ以外の出来事(友だち追加など)は今回は無視します。
    next unless event["type"] == "message"
    # テキスト以外(画像やスタンプ)も今回は無視します。
    next unless event["message"]["type"] == "text"

    # 相手が送ってきた文章を取り出します。
    received_text = event["message"]["text"]
    # 返信に必要な「返信用チケット(replyToken)」を取り出します。
    reply_token = event["replyToken"]

    # ------------------------------------------------------------------
    # ★step3 の新しいところ★
    #
    # TODO(2) 返事の内容を、届いた文章によって作り分けます。
    #
    #   このファイルの下に build_reply という関数を作ります（TODO(3)〜(6)）。
    #     def build_reply(text)
    #
    #   その関数に received_text をわたして呼び出し、
    #   返ってきた文章を reply_text という変数に入れてください。
    #
    #   ヒント: 変数 = 関数名(わたす変数) という形で書きます
    # ------------------------------------------------------------------
    reply_text = ______

    # 作った返事を送ります。
    reply_message(reply_token, reply_text)
  end

  # LINE には「ちゃんと受け取ったよ」という合図(200)を返します。
  status 200
  "OK"
end

# ---- 返す文章を決める処理（場合分け） ----

# 届いた文章(text)を受け取って、「返す文章」を返す関数です。
def build_reply(text)
  # --------------------------------------------------------------------
  # TODO(3) 届いた文章の、前後の余分な空白や改行を取り除いてください。
  #
  #   .strip は、文字列の前後の空白・改行を取り除いてくれるメソッドです。
  #     "  こんにちは  ".strip  →  "こんにちは"
  #
  #   これをしておくと「 おみくじ 」のように空白が入っていても
  #   反応できるようになります。
  #
  #   ヒント: 変数のうしろに .strip を付けます
  # --------------------------------------------------------------------
  message = ______

  # --------------------------------------------------------------------
  # TODO(4) if 〜 else 〜 end で「場合分け」をします。
  #
  #   上で作った message が、文字の "おみくじ" と同じかをくらべてください。
  #
  #   ヒント: 2つの値が同じかをくらべるのは == です
  # --------------------------------------------------------------------
  if ______
    # ------------------------------------------------------------------
    # TODO(5) 「おみくじ」だったときの返り値を書いてください。
    #
    #   このファイルの下に omikuji という関数を作ります（TODO(7)〜(8)）。
    #   その関数を呼び出すだけでOKです。
    #
    #   Ruby では、関数の中で「最後に書いた値」が
    #   そのまま返り値になります（return を書かなくても大丈夫です）。
    #
    #   ヒント: omikuji は引数を取らないので、( ) も要りません
    # ------------------------------------------------------------------
    ______
  else
    # ------------------------------------------------------------------
    # TODO(6) それ以外だったときの返り値を書いてください。
    #
    #   step2 と同じく、届いた文章をそのまま返します（オウム返し）。
    #
    #   ヒント: この関数が受け取った引数の名前を、そのまま書きます
    #           （.strip する前のものを返します）
    # ------------------------------------------------------------------
    ______
  end
end

# ---- おみくじの処理 ----

# 呼び出されるたびに、おみくじの結果を1つ作って返す関数です。
def omikuji
  # --------------------------------------------------------------------
  # TODO(7) おみくじの結果を、ランダムに1つ選んでください。
  #
  #   配列.sample は、配列の中からランダムに1つ選んでくれるメソッドです。
  #     ["あか", "あお", "きいろ"].sample  →  どれか1つ
  #
  #   一番上の TODO(1) で作った配列から1つ選び、result に入れます。
  #   （毎回ちがう結果になるのは .sample のおかげです）
  # --------------------------------------------------------------------
  result = ______

  # --------------------------------------------------------------------
  # TODO(8) 返す文章を組み立ててください。
  #
  #   "#{ }" の中には、変数の中身をそのまま埋め込めます。
  #   \n は「ここで改行する」という意味の書き方です。
  #
  #   下の文章の ______ の所に、上で選んだ変数を埋め込んでください。
  #   （完成すると LINE にはこう届きます）
  #
  #     🎋 今日の運勢は…
  #
  #     【 大吉 】
  #
  #     また引きたいときは「おみくじ」と送ってね！
  # --------------------------------------------------------------------
  "🎋 今日の運勢は…\n\n【 #{______} 】\n\nまた引きたいときは「おみくじ」と送ってね！"
end

# ---- 届いた連絡が本物かを確認する処理 ----
# ここは完成済みです。中身がむずかしいので、今は読まなくて大丈夫です。

def valid_signature?(body, signature)
  # シークレットが無いと確認できないので、その場合は false を返します。
  return false if CHANNEL_SECRET.nil? || CHANNEL_SECRET.empty?
  return false if signature.nil?

  # シークレットを鍵にして、本文から「正しい署名」を自分で計算します。
  hash = OpenSSL::HMAC.digest("SHA256", CHANNEL_SECRET, body)
  expected = Base64.strict_encode64(hash)

  # LINE から届いた署名と、自分で計算した署名が一致すれば本物です。
  # secure_compare は、安全に文字列を比べるための方法です。
  Rack::Utils.secure_compare(expected, signature)
end

# ---- 返信する処理 ----
# ここも完成済みです。

def reply_message(reply_token, text)
  # 返信専用の窓口(URL)です。step1 の broadcast とは別の住所です。
  uri = URI.parse("https://api.line.me/v2/bot/message/reply")

  request = Net::HTTP::Post.new(uri)
  request["Content-Type"] = "application/json"
  request["Authorization"] = "Bearer #{CHANNEL_ACCESS_TOKEN}"

  # 返信では「どの連絡への返事か(replyToken)」を必ず指定します。
  request.body = {
    replyToken: reply_token,
    messages: [
      { type: "text", text: text }
    ]
  }.to_json

  # 実際に返信を送ります。
  Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
    http.request(request)
  end
end
