# =====================================================================
# 【step4・穴埋め版】届いたメッセージの「種類(タイプ)」を判別する
# プログラムです。
#
# step3（おみくじ）の続きです。step3 の機能はそのまま残したまま、
# 「文字以外」にも反応できるようにします。
#
# step3 までは「文字(text)」だけを見ていて、画像やスタンプは
# next で読み飛ばしていました。
# step4 では読み飛ばさずに、何が届いたのかを判別して返事をします。
#
#   ・「おみくじ」と送られたら → omikuji の結果を返す  ← step3 から引き継ぎ
#   ・その他の文字が届いたら   → 「文字列です」＋ 内容（オウム返し）
#   ・画像が届いたら           → 「画像です」          ← step4 で追加
#   ・スタンプが届いたら       → 「スタンプです」      ← step4 で追加
#   ・…など
#
# ★このファイルは未完成です★
#   ______ となっている所を Ruby のコードで埋めると動きます。
#   TODO は全部で 10 個あります。上から順番に埋めていきましょう。
#
# このステップで練習する Ruby の基礎:
#   TODO(1) Hash をまるごと取り出して変数に入れる
#   TODO(2) Hash を引数として関数にわたす
#   TODO(3) Hash から値を取り出す
#   TODO(4) case で「何を見て分けるか」を指定する
#   TODO(5) when の中から別の関数を呼ぶ（2段の場合分け）
#   TODO(6) when を自分で増やす
#   TODO(7) Hash の値を文字列に埋め込む
#   TODO(8) 同じく、文字列に埋め込む
#   TODO(9) else で「どれにも当てはまらないとき」を受け止める
#   TODO(10) メソッド（.length）の結果を文字列に埋め込む
#
# 実行方法（ターミナルで）:
#   cd draft/step4
#   ruby app.rb
#   （すでに別の step のフォルダにいる場合は、先に cd ../../ でルートに戻ってください）
#   → その後、Codespaces で公開されたURLを LINE の Webhook に登録します
#     （くわしくは draft/step4/README.md を参照）
#
# 答えを見たいときは ../../complete/step4/app.rb を開いてください。
# =====================================================================

# ---- 使う道具（ライブラリ）を読み込みます ----
# ここは完成済みです。使う道具は step3 とまったく同じです。

# sinatra は、小さな Web サーバーを作るための道具です。
require "sinatra"
# net/http / uri / json は、LINE への送信に使います。
require "net/http"
require "uri"
require "json"
# openssl と base64 は、届いた連絡が本当に LINE からのものか
# 確認する「署名チェック」に使う道具です。
require "openssl"
require "base64"
# dotenv は、.env ファイルを読み込んで ENV に入れてくれます。
# リポジトリルートの .env を直接指定して読み込みます。
require "dotenv"
Dotenv.load(File.expand_path("../../.env", __dir__))

# ---- 秘密の設定を環境変数から取り出します ----
# ここも完成済みです。

# メッセージを送る（返信する）ための鍵です。
CHANNEL_ACCESS_TOKEN = ENV["LINE_CHANNEL_ACCESS_TOKEN"]
# 届いた連絡が本物か確認するための、もう1つの鍵(シークレット)です。
CHANNEL_SECRET = ENV["LINE_CHANNEL_SECRET"]

# ---- おみくじの中身を用意します（step3 から引き継ぎ） ----
# step3 で作ったものをそのまま持ってきています。完成済みです。

# [ ] で囲んだものを「配列(はいれつ)」と呼びます。
# ここに書いた中から1つが選ばれます。増やしても減らしてもOKです。
OMIKUJI_RESULTS = [
  "大吉",
  "中吉",
  "小吉",
  "吉",
  "末吉",
  "凶",
  "大凶"
]

# ---- サーバーの待ち受け設定 ----
# ここは完成済みです。おまじないだと思って先に進みましょう。

# どのパソコンからの接続も受け付ける、という意味の設定です。
# （Codespaces で外から届くようにするために必要です）
set :bind, "0.0.0.0"
# 待ち受けるポート番号です。4567 は sinatra の初期値です。
set :port, 4567
# どの住所(ホスト名)からのアクセスも許可する、という設定です。
# sinatra は安全のため、初期状態だと localhost 以外を
# 「Host not permitted」と拒否します。Codespaces の公開URL
# （...app.github.dev）で受け取れるように、この制限を外します。
set :host_authorization, { permitted_hosts: [] }

# ---- 動作確認用のページ ----

# ブラウザで「/」を開いたときに表示される簡単な文字です。
# サーバーが動いているかの確認に使えます。
get "/" do
  "おみくじ＋タイプ判別Bot は動いています（step4）"
end

# ---- LINE からの連絡(Webhook)を受け取る入り口 ----

# LINE は「/callback」という住所に POST で連絡してきます。
post "/callback" do
  # 届いた本文（まだ文字のかたまり）を読み取ります。
  body = request.body.read

  # まず、この連絡が本当に LINE からのものかを確認します。
  # にせものを受け付けないための、大切なセキュリティ確認です。
  unless valid_signature?(body, request.env["HTTP_X_LINE_SIGNATURE"])
    halt 400, "署名が正しくありません"
  end

  # 文字のかたまり(JSON)を、Ruby が扱いやすい形に変換します。
  data = JSON.parse(body)

  # 1回の連絡に複数の出来事(events)が入っていることがあるので、
  # each で1つずつ順番に処理します。
  data["events"].each do |event|
    # メッセージ以外の出来事(友だち追加など)は今回は無視します。
    next unless event["type"] == "message"

    # ★step4 の新しいところ★
    # step3 までは、ここに
    #   next unless event["message"]["type"] == "text"
    # と書いて「テキスト以外は無視」していました。
    # step4 ではこの行を消して、画像やスタンプも受け取ります。

    # 返信に必要な「返信用チケット(replyToken)」を取り出します。
    reply_token = event["replyToken"]

    # ------------------------------------------------------------------
    # TODO(1) メッセージの中身を、まるごと取り出して変数に入れてください。
    #
    #   step3 では event["message"]["text"] と書いて、
    #   中の「文字」だけを取り出していました。
    #   step4 では種類も見たいので、"message" の Hash ごと取り出します。
    #
    #     event = {
    #       "type" => "message",
    #       "replyToken" => "abcd1234",
    #       "message" => { "type" => "text", "text" => "こんにちは" }
    #                     ↑ この { } をまるごと取り出します
    #     }
    #
    #   ヒント: [ ] は1回だけで、"text" は付けません
    # ------------------------------------------------------------------
    message = ______

    # ------------------------------------------------------------------
    # TODO(2) 返す文章を組み立ててください。
    #
    #   step3 では build_reply に「文字」だけを渡していましたが、
    #   step4 では種類も見たいので、上で取り出した Hash ごと渡します。
    #
    #   ヒント: 変数 = 関数名(わたす変数) という形で書きます
    # ------------------------------------------------------------------
    reply_text = ______

    # 作った返事を送ります。
    reply_message(reply_token, reply_text)
  end

  # LINE には「ちゃんと受け取ったよ」という合図(200)を返します。
  status 200
  "OK"
end

# ---- 【1段目の場合分け】届いたメッセージの種類を判別する ----

# message には、LINE から届いた「メッセージの中身」が入っています。
# 例）文字が届いたとき:
#   { "type" => "text", "id" => "123", "text" => "こんにちは" }
# 例）スタンプが届いたとき:
#   { "type" => "sticker", "id" => "456", "packageId" => "789", ... }
#
# このように、キー（"type" など）と値（"text" など）が
# セットになっている入れ物を Hash（ハッシュ）と呼びます。
# message["type"] と書くと、"type" に対応する値を取り出せます。
def build_reply(message)
  # --------------------------------------------------------------------
  # TODO(3) メッセージの種類を取り出して、変数に入れてください。
  #
  #   どの種類のメッセージにも必ず "type" というキーが入っています。
  #   その値には "text" や "image" や "sticker" などが入ります。
  # --------------------------------------------------------------------
  type = ______

  # --------------------------------------------------------------------
  # TODO(4) case 〜 when 〜 end は、if / elsif をたくさん並べるかわりに
  #         「この値だったらこれ」とすっきり書ける書き方です。
  #
  #   case のうしろには「何を見て分けるか」を書きます。
  #   上で作った変数を書いてください。
  #
  #   書き方のイメージ:
  #     case くだもの
  #     when "りんご"
  #       "赤いです"
  #     when "ばなな"
  #       "黄色いです"
  #     else
  #       "わかりません"
  #     end
  # --------------------------------------------------------------------
  case ______
  when "text"
    # ------------------------------------------------------------------
    # TODO(5) 文字のときだけ、さらに細かい判定（2段目）にわたします。
    #
    #   このファイルの下に reply_for_text という関数があります。
    #     def reply_for_text(text)
    #
    #   この関数に「届いた文字」をわたして呼び出してください。
    #   引数には Hash ではなく、その中の「文字」だけをわたします。
    #   ここで step3 のおみくじ機能につながります。
    #
    #   ヒント: message から "text" の値を取り出してわたします
    # ------------------------------------------------------------------
    ______
  when "image"
    # ↓ これがお手本です。次の TODO(6) で真似して増やします。
    "画像です。"
  # --------------------------------------------------------------------
  # TODO(6) 動画・音声・スタンプのときの返事を、3つ追加してください。
  #
  #   すぐ上の when "image" と同じ形で、3セット書きます。
  #     ・"video"   のとき → "動画です。"
  #     ・"audio"   のとき → "音声です。"
  #     ・"sticker" のとき → "スタンプです。"
  #
  #   書き方:
  #     when "video"
  #       "動画です。"
  #
  #   ※ この ______ の行を消して、そこに3セット書いてください
  # --------------------------------------------------------------------
  ______
  when "file"
    # ------------------------------------------------------------------
    # TODO(7) ファイルのときは、ファイル名も届きます。
    #
    #   message の中に "fileName" というキーで入っています。
    #   下の文章の ______ に、それを取り出す式を書いてください。
    #
    #   ヒント: #{ } の中には、message["キー名"] のような式も書けます
    # ------------------------------------------------------------------
    "ファイルです。\n\nファイル名: #{______}"
  when "location"
    # ------------------------------------------------------------------
    # TODO(8) 位置情報のときは、住所や緯度経度も届きます。
    #
    #   住所は "address" というキーで入っています。
    #   TODO(7) と同じ形で書いてみましょう。
    # ------------------------------------------------------------------
    "位置情報です。\n\n住所: #{______}"
  else
    # ------------------------------------------------------------------
    # TODO(9) どの when にも当てはまらなかったときの返事を書いてください。
    #
    #   LINE は将来あたらしい種類を増やすことがあります。
    #   知らない種類が来ても止まらないように、else を用意しておきます。
    #
    #   次のような文章を返してください（〇〇には届いた種類が入ります）:
    #     知らない種類（〇〇）が届きました。
    #
    #   ヒント: TODO(3) で作った変数を #{ } で埋め込みます
    # ------------------------------------------------------------------
    ______
  end
end

# ---- 【2段目の場合分け】文字が届いたときの返事を決める ----

# ここは step3 の build_reply とほとんど同じ中身です。
# 「おみくじ」なら omikuji、それ以外なら中身を返します。
def reply_for_text(text)
  # strip は、前後の余分な空白や改行を取り除いてくれます。
  # 「 おみくじ 」のように空白が入っていても反応できるようにします。
  message = text.strip

  if message == "おみくじ"
    # step3 と同じく、おみくじの結果を返します。
    omikuji
  else
    # ------------------------------------------------------------------
    # TODO(10) おみくじ以外の文字が届いたときの返事を組み立ててください。
    #
    #   step4 では「文字列です」と種類を伝えつつ、
    #   step2・step3 のオウム返しも一緒に返します。
    #
    #   .length は文字数を数えてくれるメソッドです。
    #     "こんにちは".length  →  5
    #
    #   下の文章の2つの ______ を埋めてください。
    #     ・1つ目 … 届いた文章そのもの
    #     ・2つ目 … 届いた文章の文字数
    #
    #   完成すると LINE にはこう届きます（「やっほー」と送った場合）:
    #     文字列です。
    #
    #     内容: やっほー
    #     文字数: 4文字
    # ------------------------------------------------------------------
    "文字列です。\n\n内容: #{______}\n文字数: #{______}文字"
  end
end

# ---- おみくじの処理（step3 から引き継ぎ） ----
# step3 で作ったものをそのまま持ってきています。完成済みです。

# 呼び出されるたびに、おみくじの結果を1つ作って返す関数です。
def omikuji
  # 配列.sample は、配列の中からランダムに1つ選んでくれます。
  # （毎回ちがう結果になるのはこのおかげです）
  result = OMIKUJI_RESULTS.sample

  # 返す文章を組み立てます。
  # "#{ }" の中には、変数の中身をそのまま埋め込めます。
  # \n は「ここで改行する」という意味の書き方です。
  "🎋 今日の運勢は…\n\n【 #{result} 】\n\nまた引きたいときは「おみくじ」と送ってね！"
end

# ---- 届いた連絡が本物かを確認する処理 ----
# ここは完成済みです。中身がむずかしいので、今は読まなくて大丈夫です。

def valid_signature?(body, signature)
  # シークレットが無いと確認できないので、その場合は false を返します。
  return false if CHANNEL_SECRET.nil? || CHANNEL_SECRET.empty?
  return false if signature.nil?

  # シークレットを鍵にして、本文から「正しい署名」を自分で計算します。
  hash = OpenSSL::HMAC.digest("SHA256", CHANNEL_SECRET, body)
  expected = Base64.strict_encode64(hash)

  # LINE から届いた署名と、自分で計算した署名が一致すれば本物です。
  # secure_compare は、安全に文字列を比べるための方法です。
  Rack::Utils.secure_compare(expected, signature)
end

# ---- 返信する処理 ----
# ここも完成済みです。

def reply_message(reply_token, text)
  # 返信専用の窓口(URL)です。step1 の broadcast とは別の住所です。
  uri = URI.parse("https://api.line.me/v2/bot/message/reply")

  request = Net::HTTP::Post.new(uri)
  request["Content-Type"] = "application/json"
  request["Authorization"] = "Bearer #{CHANNEL_ACCESS_TOKEN}"

  # 返信では「どの連絡への返事か(replyToken)」を必ず指定します。
  request.body = {
    replyToken: reply_token,
    messages: [
      { type: "text", text: text }
    ]
  }.to_json

  # 実際に返信を送ります。
  Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
    http.request(request)
  end
end
